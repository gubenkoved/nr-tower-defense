<UserControl x:Class="NRTowerDefense.GameActionButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="55" d:DesignWidth="199">
    <Grid>
        <Button HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" Click="OnButtonClick" Template="{StaticResource TowerButton}" BorderBrush="#FF868686" SnapsToDevicePixels="True">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Image Stretch="Uniform" Source="/NRTowerDefense;component/images/skull.png" Name="image" Width="26" Height="26" />
                <Label Grid.Column="1" Content="ActionText" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Name="actionLabel" />
            </Grid>
        </Button>
    </Grid>
</UserControl>
